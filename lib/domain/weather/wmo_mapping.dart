/// WMO code + is_day + wind speed to one condition word.
///
/// Arrives in M3. Placed here now so the layer it belongs to is decided
/// before the code is written — ARCHITECTURE.md §1 and §2.

library;
