<?xml version="1.0" encoding="utf-8"?>
<!-- The launch window, on API 24 to 30 and behind the system splash after it.
     The paper ground, not white: the app has no light theme, and the ground
     the icon is drawn on is the one the first screen opens on (ADR-090). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/ic_launcher_background" />
</layer-list>
